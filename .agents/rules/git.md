# Git Remote Configuration

When asked to push to GitHub for this project:
- Always push to `origin` which is mapped to `https://github.com/Saurabh-1706/PaperLink.git`.
- Do not push to `answermap` / `VedaAI-task` unless explicitly requested.
