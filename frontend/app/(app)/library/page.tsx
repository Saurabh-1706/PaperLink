import PlaceholderPage from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Library · VedaAI" };

export default function LibraryPage() {
  return (
    <PlaceholderPage
      icon="local_library"
      title="Library"
      description="Question papers and answer sheets from past classes, ready to reuse in a new assessment."
    />
  );
}
