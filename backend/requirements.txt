# Core API
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.5
python-multipart>=0.0.12
email-validator>=2.2

# Auth
pyjwt>=2.9
argon2-cffi>=23.1

# Persistence (MongoDB; GridFS ships inside pymongo)
pymongo>=4.9

# Async processing
celery>=5.4
redis>=5.1

# Documents / extraction
pymupdf>=1.24
pillow>=10.4
opencv-python-headless>=4.10  # photographed-page auto-crop/perspective-rectify (documents/rectify.py)

# OCR (imported lazily; selected by OCR_ENGINE env var — ADR-004).
# paddle* is excluded: it conflicts with onnxruntime on Python 3.12 and is not
# used when OCR_ENGINE=rapid. Add it back only if switching to OCR_ENGINE=paddle.
rapidocr-onnxruntime>=1.3

# Mapping / scoring
scipy>=1.14
numpy>=2.0
rapidfuzz>=3.9

# Orchestration + LLM utilities
langgraph>=0.2
langchain-core>=0.3
langchain-google-genai>=2.0
langchain-groq>=0.3
